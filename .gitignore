# OAuth secrets — never commit (provided via GitHub Secrets on CI)
client_secret.json
token.json
token.json.bak

# Working files
runs/
logs/*
!logs/history.csv
__pycache__/
*.pyc

# Twitch app credentials for local runs (CI uses GitHub Secrets)
.env

# local render checks
sample_short.mp4
frame.png
